

/**
 * Module dependencies.
 */

const {
  Config,
  Logger,
  Postgres,
  Redis,
} = require('../services')

const {
  AuthService,
  ProfileService,
} = require('../components')

class Dependencies {
  constructor() {

  }
}

Dependencies.prototype.Dependencies = Dependencies

module.exports = new Dependencies()
