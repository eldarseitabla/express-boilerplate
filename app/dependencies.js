

/**
 * Module dependencies.
 */

const {
  Config,
  Logger,
  Postgres,
  Redis,
} = require('../services')

const {
  AuthService,
  ProfileService,
} = require('../services')

class Dependencies {
  constructor() {

  }
}

module.exports = Dependencies
