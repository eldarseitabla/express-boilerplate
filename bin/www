#!/usr/bin/env node

'use strict'

if (!process.env.LOG4JS_CONFIG) {
  process.env.LOG4JS_CONFIG = 'config/log4js.dev.json'
}

const config = require('../config')

const { App } = require('../app')
const app = new App(config);

(async function() {
  await app.run(config.clusterMode)
}())
