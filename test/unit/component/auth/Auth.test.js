'use strict'

const assert = require('assert')

describe('Auth', () => {
  it('signUp', () => {
    assert.strictEqual(true, true)
  })
  it('authenticate', () => {
    assert.strictEqual(true, true)
  })
  it('facebookCallback', () => {
    assert.strictEqual(true, true)
  })
})
